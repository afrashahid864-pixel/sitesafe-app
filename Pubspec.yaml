name: sitesafe_app
description: Website Safety Checker
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.2
  google_mobile_ads: ^4.0.0

flutter:
  uses-material-design: true